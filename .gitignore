# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version control
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Exclude .terraform.lock.hcl as it contains provider version constraints
.terraform.lock.hcl

# Exclude terraform binary
terraform.exe

# Exclude PEM files (private keys)
*.pem

# Exclude all .pub key files
*.pub

# Exclude any private key files
*private_key* 